default:
		@echo "--> Setting up local environment ============================================"
		./setup_local.sh

clean:
		@echo "--> Setting up local environment ============================================"
		rm -Rvf app/build/*
